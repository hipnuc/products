# Stable device name and access for HiPNUC evaluation boards (CP210x bridge).
# Install: sudo cp 99-hipnuc.rules /etc/udev/rules.d/ && sudo udevadm control --reload && sudo udevadm trigger
SUBSYSTEM=="tty", ATTRS{idVendor}=="10c4", ATTRS{idProduct}=="ea60", MODE="0666", SYMLINK+="hipnuc"
