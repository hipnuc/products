find_package(Python3 REQUIRED COMPONENTS Interpreter)
set(TEST_COMMAND_SOURCES
  ../cli.c ../commands.c ../update.c ../j1939_reg_api.c fake_can.c)
add_executable(canhost_fake ../main.c ${TEST_COMMAND_SOURCES})
add_executable(canhost_record_test test_record.c ${TEST_COMMAND_SOURCES})
foreach(target canhost_fake canhost_record_test)
  target_include_directories(${target} PRIVATE ..)
  target_link_libraries(${target} PRIVATE hipnuc_json hipnuc_j1939 hipnuc_update hipnuc_tool_common m)
  target_link_options(${target} PRIVATE -Wl,--wrap=fclose)
  target_compile_options(${target} PRIVATE -Wall -Wextra -Werror -UNDEBUG)
  set_target_properties(${target} PROPERTIES C_STANDARD 99 C_STANDARD_REQUIRED YES)
endforeach()
add_test(NAME canhost_commands COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_cli.py $<TARGET_FILE:canhost_fake>)
add_test(NAME canhost_large_json COMMAND canhost_record_test)
